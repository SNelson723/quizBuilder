const Cards = ({ card }) => {
  const answers = card.incorrect_answers;
  const correct_answer = card.correct_answer;
  console.log(card);
  return (
    <>
    </>
  );
};

export default Cards;